

import Logo from '../Logo/Logo'
import Nav from '../Header/Nav/Nav'

function Header (props){
    return (
    <header>
        <div>MENU</div>
        <Logo />
        <Nav />
    </header>
    )
    
}

export default Header